/**
 * Enregistrer ici uniquement des adaptateurs locaux révisés.
 * Un adaptateur expose :
 * { id, supports({ type }), resolve({ type, imdbId, season, episode }) }
 *
 * Ne pas charger ni exécuter JavaScript provenant d’une URL de dépôt à la volée.
 */
export const providers = [];
